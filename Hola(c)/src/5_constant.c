#include<stdio.h>
int main()
{

const int minutesPerHour = 60;
const float PI = 3.14;
//If you don't want others (or yourself) to override existing variable values, use the const keyword 
//(this will declare the variable as "constant", which means unchangeable and read-only)
return 0;
}