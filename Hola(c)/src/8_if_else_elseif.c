#include <stdio.h>
int main()
{
    int x = 0;

    if (x < 0)
    {
        printf("x is -ve");
    }

    else if (x > 0)
    {
        printf("x is +ve");
    }

    else
    {
        printf("is 0  ");
    }

    return 0;
}